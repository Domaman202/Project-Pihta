(
    (use std)
    (ns pht.std
        (defmacro ctor [args body] (fn <init> ^void (macro-arg args) (macro-arg body)))
        (defmacro let [var body] (body (def (macro-arg var)) (macro-arg body)))
    )
)