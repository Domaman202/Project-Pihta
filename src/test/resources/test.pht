(
    (use std/base std/util)

    (import [java.lang.Object Any][java.lang.String String])

    (ns ru.DmN.test (
        (obj Main [^Any] (
            (defn main ^Any [] (
                (def [[str "Text"]])
                (#invoke (rfn [str][] (#println (use std/base) str)))
                (unit)))))
    ))
)