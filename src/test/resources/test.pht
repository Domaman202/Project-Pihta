(use-ctx pht
    (import java
        (type java.lang.Runnable))
    (app
        (defn test ^void [[o ^Runnable]]
            (#run o))
        (field [[i ^int]])
        (defn main ^void [] (progn
            (set i 12)
            (#test ^App (rfn [i][] (println i)))))))