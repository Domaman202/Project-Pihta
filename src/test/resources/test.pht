(
    (use std/base std/util)

    (import [java.lang.Object Any][java.lang.String String])

    (ns ru.DmN.test (
        (obj Main [^Any] (
            (defn main ^Any [] (
                (#invoke (fn [] (#println (use std/base) "text")))
                (unit)))))
    ))
)