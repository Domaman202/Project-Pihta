(
    (use std/base std/util)

    (import [java.lang.Object Any][java.lang.String String])

    (ns ru.DmN.test (
        (obj Main [^Any] (
            (defn main ^Any [] (
                (#run (fn ^java.lang.Runnable [] (#println (use std/base) "text")))
                (unit)))))
    ))
)