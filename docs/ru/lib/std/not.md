## not
Инвертирует __логическое__ `значение`.

### Применение

1. `(not val)`<br>
`val` - _значение_.<br><br>
2. `(! val)`<br>
`val` - _значение_.

### Примеры

```pihta
(use-ctx pht
    (app-fn
        (println (! true))))
```