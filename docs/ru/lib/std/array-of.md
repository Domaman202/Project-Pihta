## array-of
Создаёт `массив` из `элементов`.

### Применение

1. `(array-of (expr0) (exprN))`<br>
`(expr0)` `(exprN)` - _элементы_.

### Примеры

```pihta
(use-ctx pht
    (app-fn
        (println (array-of 12 21 33))))
```