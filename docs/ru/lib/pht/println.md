## println
Выводит `данные` в консоль.<br>
Переводит вывод на новую строчку.

### Применение

1. `(println data)`<br>
`data` - _данные_.

### Примеры

```pihta
(use-ctx pht
    (app-fn
        (println "#FuckNato")
        (println "Слава России!")))
```