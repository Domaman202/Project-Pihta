## new-longs
Создаёт статический массив типа `long`.

### Применение

1. `(new-longs i)`<br>
`i` - _размер массива_.

### Примеры

```pihta
(use std/collections)
(#println (use std/base) (new-longs 2022))
```