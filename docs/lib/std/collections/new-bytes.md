## new-bytes
Создаёт статический массив типа `byte`.

### Применение

1. `(new-bytes i)`<br>
`i` - _размер массива_.

### Примеры

```pihta
(use std/collections)
(#println (use std/base) (new-bytes 2024))
```