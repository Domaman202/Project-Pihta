## new-floats
Создаёт статический массив типа `float`.

### Применение

1. `(new-floats i)`<br>
`i` - _размер массива_.

### Примеры

```pihta
(use std/collections)
(#println (use std/base) (new-floats 404))
```