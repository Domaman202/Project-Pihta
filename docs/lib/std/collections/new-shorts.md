## new-shorts
Создаёт статический массив типа `short`.

### Применение

1. `(new-shorts i)`<br>
`i` - _размер массива_.

### Примеры

```pihta
(use std/collections)
(#println (use std/base) (new-shorts 777))
```