## new-booleans
Создаёт статический массив типа `boolean`.

### Применение

1. `(new-booleans i)`<br>
`i` - _размер массива_.

### Примеры

```pihta
(use std/collections)
(#println (use std/base) (new-booleans 1945))
```