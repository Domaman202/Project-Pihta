## new-ints
Создаёт статический массив типа `int`.

### Применение

1. `(new-ints i)`<br>
`i` - _размер массива_.

### Примеры

```pihta
(use std/collections)
(#println (use std/base) (new-ints 999))
```