## new-chars
Создаёт статический массив типа `char`.

### Применение

1. `(new-chars i)`<br>
`i` - _размер массива_.

### Примеры

```pihta
(use std/collections)
(#println (use std/base) (new-chars 202))
```