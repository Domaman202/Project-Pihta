## ->
Свертывает `выражения` __справо-налево__.

### Применение

1. `(-> (expr0) (exprN))`
`(expr0)` `(exprN)` - _выражения_.

### Примеры

```pihta
(use-ctx pht
    (app-fn
        (println (<- 1 (+ 1) (* 2)))))
```