## typeof
Выводит `тип` значения `выражения`.<br>
Вычисляется на этапе компиляции.

### Применение

1. `(typeof 12)`<br>
`12` - _выражение_.

### Примеры

```pihta
(#println (use std/base) (typeof "СССР"))
```