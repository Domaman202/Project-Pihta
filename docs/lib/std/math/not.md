## !
Инвертирует булевое `значение` `выражения`.

### Применения

1. `(! (expr))`<br>
`expr` - _выражение_.

### Примеры

```pihta
(use std/base std/math)
(#println std
    (! true)
    (! false))
```