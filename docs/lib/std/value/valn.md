## valn
Список `выражений`.

### Применение

1. `(valn (expr0) (exprN))`<br>
`(expr0)` `(exprN)` - _выражения_.<br><br>
2. `[(expr0) (exprN)]`<br>
`(expr0)` `(exprN)` - _выражения_.

### Примеры

```pihta
(def [[i] [j]])
```

```pihta
(def (valn (valn n) (valn m)))
```