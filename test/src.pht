(use-ctx pht
	(use-ctx pht
		(cls [] App [^java.lang.Object]
			(@static
				(@final
					(@final
						(@static
							(defn [] getI ^int []
								(fget
									^App
									i))
							(def [
								[i ^int]]))))
				(defn [] <clinit> ^void []
					(fset
						^App
						i
						333))
				(@static
					(defn [] main ^void []
						(mcall
							^ru.DmN.pht.std.utils.StdOut
							println
							(mcall
								^App
								getI))))))))