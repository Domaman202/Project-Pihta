(progn
    (cls TestC [^Object]
        (def [[i ^int]])
        (ctor [[i ^int]]
            (ccall)
            (set this/i i))
        (@inline (defn bar ^int [[other ^int]] (+ this/i other))))
    (app-fn (println (#bar (new ^TestC 1900) 45))))