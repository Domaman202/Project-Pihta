(app
    (cls Test [^Object]
        (ctor [] (ccall))
        (def [[i ^dynamic]]))
    (app-fn
        (def [[^dynamic test (new ^Test)]])
        (set test/i "Hi!")
        (println test)
        (println test/i)))